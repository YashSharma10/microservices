spring.application.name=configurationserver
server.port=8888

spring.security.user.name=admin
spring.security.user.password=admin

# Git-backed config (used when CONFIG_MODE != native)
spring.cloud.config.server.git.username=${GIT_USERNAME}
spring.cloud.config.server.git.password=${GIT_TOKEN}
spring.cloud.config.server.git.uri=https://github.com/writetonsharma/ncu-microservices-config
spring.cloud.config.server.git.search-paths=carbonservice, tradeservice, userservice, apigateway, servicediscovery, authservice
