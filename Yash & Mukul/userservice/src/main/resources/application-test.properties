# Test configuration - using MySQL for testing (same as production)
spring.datasource.url=jdbc:mysql://localhost:3306/carboncreditdb_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=8076107814@Yash
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Initialize schema from classpath schema.sql for tests
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
